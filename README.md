# This is modified to have python bindings for the original DecompUtil which was header only in C++

Using pybind, you can create a convex polygon from a point cloud, and then get the equations for the lines.
